import {
    createRegulatorProxy
} from './upgradeRegulator/createRegulatorProxy'
import {
    claimOwnershipRegulator
} from './upgradeRegulator/claimOwnershipRegulator'
import {
    setMinter
} from './upgradeRegulator/setMinter'

const main = async () => {

    // Create a new RegulatorProxy, claim ownership over it, and set a minter
    // await createRegulatorProxy()
    // await claimOwnershipRegulator()
    // await setMinter()

    return
}

main().then(result => {
    process.exit()
})
